{
  "name": "skip-sensei",
  "version": "0.1.0",
  "private": true,
  "description": "Skip YouTube ads and AI-detected creator sponsor segments, and block ads & trackers across the web.",
  "type": "module",
  "scripts": {
    "dev": "npm run rulesets && vite",
    "build": "npm run rulesets && tsc --noEmit && vite build",
    "rulesets": "node scripts/build-rulesets.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@adguard/dnr-rulesets": "^4.1.20260707190042",
    "@crxjs/vite-plugin": "^2.0.0-beta.31",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
